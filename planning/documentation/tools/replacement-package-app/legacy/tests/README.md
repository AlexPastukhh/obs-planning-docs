# Legacy Replacement Package App Tests

Status: legacy historical implementation evidence / not current executable proof

`ChatBridgeTests.java` and `CoreTests.java` target an older runtime/API shape and are intentionally outside `src/test/java` and `build.cmd`. They must not be cited as current passing regression evidence. Git history plus these preserved sources retain the older proof intent.

Current executable proof is the test set compiled and run by `build.cmd` / `run-tests.cmd`.
