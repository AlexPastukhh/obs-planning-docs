# SOURCE

Place exact source artifacts / Repository Snapshots for the current Work here. Multiple repository sources are allowed when the Work genuinely spans them.

The selected source identity for the current source-sensitive pass must be explicit in `ALWAYS-READ.md` (and later in durable Session State). Historical snapshots may remain available but are never implicitly current.
