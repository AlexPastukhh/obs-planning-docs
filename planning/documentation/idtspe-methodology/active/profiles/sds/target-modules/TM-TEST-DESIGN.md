# TM-TEST-DESIGN — RETIRED Compatibility Route

Status: **RETIRED — not an active SDS Target Module**

Proof design is not a generic durable Test Target family.

Current routing:

```text
proof property/layer/setup/action/observation/assertion questions
→ Core LENS-TEST-PROOF-EVIDENCE

transient non-trivial owner-local proof design
→ current owner working state / Exact working plan

literal test code
→ Core TM-EXACT-REALIZATION / implementation-native tests

real implementation/environment observation
→ TM-PRACTICAL-TEST
```

Persist a proof-plan/supporting artifact only when it has independent continuing review/handoff value, under an explicitly resolved semantic owner.

No active Test Design Result Units remain here.

This file is intentionally retained only as a stable compatibility route. It is excluded from the active SDS Target Module registry, must never be scanned or instantiated as an active Target Module, and may be physically removed only during a later repository-cleanup pass after proving that no live command, helper, document, or external consumer still depends on the path.
