# Legacy / Pending Reconciliation

This area preserves exact pre-fundamental root navigation contracts as provenance.

The following exact snapshots were created from the repository state that preceded the fundamental Use-Case migration:

- `README.pre-fundamental-uc.md`;
- `use-case-registry.pre-fundamental-uc.md`.

They are historical snapshots, not current semantic owners.

Because the files were preserved verbatim under a different directory, relative links inside them reflect their former canonical location and may not resolve correctly from `planning/legacy/`. Use the current [`../README.md`](../README.md) and [`../use-case-registry.md`](../use-case-registry.md) for live navigation.

Existing repository capabilities and families are not automatically legacy merely because these navigation snapshots exist. They remain current pending explicit `KEEP / FOLD / MOVE / RETIRE` decisions.

Logs are not part of this migration.
