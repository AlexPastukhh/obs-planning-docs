# Methodology snapshot provenance

This directory originated as a byte-for-byte copy of `planning/documentation/idtspe-methodology/`
from the user-supplied `obs-planning-docs-main (100).zip`. This provenance file and
the deliberate Concept-first revision below are exceptions to original byte identity. Source archive SHA-256:
`C3188D0857C6A759092DA5D085DC398EF041520F7D660BB708306E2FACBA2311`.

The copy provides stable, exact Markdown targets for this project's
`**Methodology:**` Unit bindings. It is a pinned reference to that snapshot;
future methodology revisions require deliberate review and replacement of the
copy and its links. The application owners outside this directory retain their
own application semantics.

The source repository included planning files outside this copied methodology
subtree. This embedded copy has 63 relative-link occurrences to 15 such files,
so full source-repository navigation requires the supplied archive. The exact
Unit Definition targets used by this project are present and verified.
Source/history references under `sources-readonly/` retain their original
archive notation.

The source `MANIFEST.json` projects 12 SDS Target Modules, while the
[authoritative active SDS registry](active/profiles/sds/registries/TARGET-MODULE-REGISTRY.md)
lists 13. Treat the registry as the governance owner and the manifest as a
stale machine-readable projection; this pinned source copy is not edited to
hide the discrepancy.

## 2026-09-24 — Deliberate Concept-first revision

USER requested that Concept become the first Application Definition Unit and that the project, copied example and Target Module model be corrected together. `active/profiles/sds/target-modules/TM-APPLICATION-DEFINITION.md` now presents stable `RU-APP-05` first and retires duplicate `RU-APP-01`; `active/profiles/sds/lenses/reusable/LENS-APPLICATION-BOUNDARY-FEASIBILITY.md` follows the five-Unit inventory. This is a bounded reviewed revision, not a refresh of all embedded governance. Existing Benefits, unrealized behavior and open evidence retain their meaning. In the packaged example, the outer snapshot manifest additionally records link normalization and editorial before/after hashes.
