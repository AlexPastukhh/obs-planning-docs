
# SourceContext Evolution — Research Capture Example

Global source: `../SDS-PLANNING-STATE/SDS-WORKSPACE-EVOLUTION.md`

```text
new capture source
  MAY add a new integration mapper/adapter
  SHOULD NOT require provider-specific branches in SourceContext semantics

richer source metadata
  MAY extend SourceContext only when the metadata becomes semantic for later use
```
