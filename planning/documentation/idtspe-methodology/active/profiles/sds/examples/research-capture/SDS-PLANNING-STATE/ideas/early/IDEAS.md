# Research Capture — Early Ideas



Status: active worked-example carry-over owner  
Planning scenario: `Research Capture` SDS run.

Rule:

```text
every entry is UNSELECTED until a later Target evaluates and accepts it
```

---

# IDEA-RC-001 — Browser Extension Capture Surface

Status:

```text
ROUTED
```

Origin:

```text
AI_PROPOSAL
```

Discovered During:

```text
Phase 01 — Need / Reality
```

Potential Need / Problem:

```text
reduce friction in the Open Solution Slot
between recognizing useful material and resuming reading
```

Idea:

```text
use a browser-extension capture surface
for selected text + source context + optional short note
```

Why Out Of Current Target Scope:

```text
Phase 01 establishes Need/Reality;
it does not select the Solution.
```

Likely Future Target:

```text
Phase 02 — Whole-Solution Discovery / Selection
```

Alternatives:

```text
existing browser send-to-notes
system share action
keyboard/tool workflow
manual-process improvement
custom app with another capture route
```

Current Selection Status:

```text
UNSELECTED
```

Routing:

```text
use as IDEA_SOURCE in Phase 02
```

---

# IDEA-RC-002 — Automatic Summarization Of Captures

Status:

```text
UNROUTED
```

Origin:

```text
AI_PROPOSAL
```

Discovered During:

```text
Phase 01 example
```

Potential Need / Problem:

```text
possibly reduce later review effort
```

Idea:

```text
automatically summarize captured fragments
```

Why Out Of Current Target Scope:

```text
does not affect the current Need/Reality framing
and may not belong to the selected future Application at all
```

Likely Future Target:

```text
UNKNOWN
possibly future review Scenario / whole-solution extension
```

Current Selection Status:

```text
UNSELECTED
```

Routing:

```text
preserve only;
do not evaluate until a real target/Need makes it relevant
```
