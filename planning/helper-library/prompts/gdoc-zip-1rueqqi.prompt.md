# Prompt — gdoc zip

Status: active Planning Helper library item
Scope: exact insertion text; not planning-command authority.

[PLANNING_HELPER_LIBRARY_ITEM]
{
  "schemaVersion": 1,
  "kind": "prompt",
  "id": "gdoc-zip-1rueqqi",
  "title": "gdoc zip",
  "text": "& { $Repo=(Get-Location).Path; if (-not (Test-Path -LiteralPath (Join-Path $Repo \".git\"))) { throw \"Run this from the repository root.\" }; $Head=(git rev-parse HEAD).Trim(); if ($LASTEXITCODE -ne 0) { throw \"git rev-parse HEAD failed\" }; $Branch=(git branch --show-current).Trim(); if ($LASTEXITCODE -ne 0) { throw \"git branch failed\" }; $Out=Join-Path $HOME (\"Downloads\\gdoc-local-snapshot-\"+$Head.Substring(0,12)+\".zip\"); $Temp=Join-Path ([System.IO.Path]::GetTempPath()) (\"gdoc-local-snapshot-\"+[guid]::NewGuid().ToString(\"N\")); $RepoSnapshot=Join-Path $Temp \"repo-snapshot\"; $SourceSnapshot=Join-Path $Temp \"source-snapshot\"; New-Item -ItemType Directory -Path $RepoSnapshot,$SourceSnapshot -Force | Out-Null; try { robocopy $Repo $RepoSnapshot /E /XD (Join-Path $Repo \".git\") /NFL /NDL /NJH /NJS /NP | Out-Null; if ($LASTEXITCODE -ge 8) { throw \"robocopy failed with code $LASTEXITCODE\" }; $HeadArchive=Join-Path $Temp \"_head.zip\"; git archive --format=zip --output=$HeadArchive HEAD; if ($LASTEXITCODE -ne 0) { throw \"git archive HEAD failed\" }; Expand-Archive -LiteralPath $HeadArchive -DestinationPath $SourceSnapshot -Force; Remove-Item -LiteralPath $HeadArchive -Force; [System.IO.File]::WriteAllText((Join-Path $Temp \"HEAD.txt\"),$Head+\"`n\",[System.Text.UTF8Encoding]::new($false)); [System.IO.File]::WriteAllText((Join-Path $Temp \"BRANCH.txt\"),$Branch+\"`n\",[System.Text.UTF8Encoding]::new($false)); function Write-GitOutput([string[]]$GitArgs,[string]$OutputPath) { $ErrPath=[System.IO.Path]::GetTempFileName(); try { $P=Start-Process -FilePath \"git\" -ArgumentList $GitArgs -WorkingDirectory $Repo -NoNewWindow -Wait -PassThru -RedirectStandardOutput $OutputPath -RedirectStandardError $ErrPath; if ($P.ExitCode -ne 0) { $Err=[System.IO.File]::ReadAllText($ErrPath,[System.Text.UTF8Encoding]::new($false)); throw (\"git \"+($GitArgs -join \" \")+\" failed: \"+$Err) } } finally { Remove-Item -LiteralPath $ErrPath -Force -ErrorAction SilentlyContinue } }; Write-GitOutput @(\"-c\",\"core.quotepath=false\",\"status\",\"--porcelain=v1\",\"--untracked-files=all\") (Join-Path $Temp \"STATUS-PORCELAIN.txt\"); Write-GitOutput @(\"-c\",\"core.quotepath=false\",\"ls-files\",\"--others\",\"--exclude-standard\") (Join-Path $Temp \"UNTRACKED-FILES.txt\"); Write-GitOutput @(\"-c\",\"core.quotepath=false\",\"ls-files\") (Join-Path $Temp \"TRACKED-FILES.txt\"); $DiffFile=Join-Path $Temp \"DIFF-HEAD.patch\"; git --no-pager diff --no-color --binary --output=$DiffFile HEAD -- .; if ($LASTEXITCODE -ne 0) { throw \"git diff HEAD failed\" }; $CachedFile=Join-Path $Temp \"DIFF-CACHED.patch\"; git --no-pager diff --cached --no-color --binary --output=$CachedFile -- .; if ($LASTEXITCODE -ne 0) { throw \"git diff --cached failed\" }; $Summary=@(\"# Local Repository Snapshot\",\"\",\"HEAD: $Head\",\"Branch: $Branch\",\"\",\"Top-level repository state:\",\"- DIFF-HEAD.patch — complete tracked working-tree diff against HEAD\",\"- DIFF-CACHED.patch — staged diff\",\"- STATUS-PORCELAIN.txt — Git working-tree status\",\"- UNTRACKED-FILES.txt — untracked non-ignored paths\",\"- TRACKED-FILES.txt — tracked paths\",\"- HEAD.txt / BRANCH.txt — repository identity\",\"\",\"Trees:\",\"- repo-snapshot/ — complete current local working tree excluding .git\",\"- source-snapshot/ — clean tracked tree at HEAD\",\"\",\"Untracked files are not represented by git diff HEAD; their complete contents are present in repo-snapshot/ and their paths are listed in UNTRACKED-FILES.txt.\"); [System.IO.File]::WriteAllLines((Join-Path $Temp \"SNAPSHOT-README.md\"),$Summary,[System.Text.UTF8Encoding]::new($false)); if (Test-Path -LiteralPath $Out) { Remove-Item -LiteralPath $Out -Force }; Add-Type -AssemblyName System.IO.Compression.FileSystem; $Zip=[System.IO.Compression.ZipFile]::Open($Out,[System.IO.Compression.ZipArchiveMode]::Create); try { Get-ChildItem -LiteralPath $Temp -Recurse -File | ForEach-Object { $Rel=$_.FullName.Substring($Temp.Length); while ($Rel.StartsWith(\"\\\") -or $Rel.StartsWith(\"/\")) { $Rel=$Rel.Substring(1) }; $Rel=$Rel.Replace(\"\\\",\"/\"); [System.IO.Compression.ZipFileExtensions]::CreateEntryFromFile($Zip,$_.FullName,$Rel,[System.IO.Compression.CompressionLevel]::Optimal) | Out-Null } } finally { $Zip.Dispose() }; $Sha=(Get-FileHash -LiteralPath $Out -Algorithm SHA256).Hash.ToLowerInvariant(); Write-Host \"Created: $Out\"; Write-Host \"HEAD: $Head\"; Write-Host \"Branch: $Branch\"; Write-Host \"SHA-256: $Sha\"; Write-Host \"Snapshot contains top-level DIFF-HEAD.patch plus repo-snapshot/ and source-snapshot/.\" } finally { Remove-Item -LiteralPath $Temp -Recurse -Force -ErrorAction SilentlyContinue } }",
  "createdAt": "2026-08-16T01:12:31.623Z",
  "updatedAt": "2026-08-16T02:00:26.100Z"
}
[/PLANNING_HELPER_LIBRARY_ITEM]
