# Prompt — реф обьекты бутстр

Status: active Planning Helper library item
Scope: exact insertion text; not planning-command authority.

[PLANNING_HELPER_LIBRARY_ITEM]
{
  "schemaVersion": 1,
  "kind": "prompt",
  "id": "item-kljpu8-f7wgiu",
  "title": "реф обьекты бутстр",
  "text": "Нужно быстро понять, как в этом репозитории работают Linked Notes Reference Objects:\n\nhttps://github.com/AlexPastukhh/obs-planning-docs\n\nЭто лёгкий ознакомительный проход.\nНе изучай весь Linked Notes runtime и не делай аудит инструмента.\n\nЦель:\nпонять repository-facing контракт Reference Objects,\nточный синтаксис obs-ref тегов,\nгде находится canonical value,\nчто хранит registry,\nи когда Reference Object использовать нельзя.\n\nЧитай только по этому пути:\n\n1. Сначала открой:\n\n   .linked-notes/AGENT-GUIDE.md\n\n   Прочитай:\n   - зачем content-working chat вообще должен знать о Linked Notes;\n   - короткое правило Reference Objects;\n   - когда нужно переходить к detailed contract.\n\n2. Затем открой:\n\n   .linked-notes/REFERENCE-OBJECTS.md\n\n   Это главный контракт Reference Objects.\n\n   Обязательно пойми:\n\n   - Reference Object имеет stable `ro_*` ID;\n   - human-readable name хранится в registry;\n   - существует ровно один canonical definition marker;\n   - может существовать 0+ materialized use markers;\n\n   точный синтаксис:\n\n   <!-- obs-ref:def id=\"ro_example\" -->VALUE<!-- /obs-ref:def -->\n\n   <!-- obs-ref:use id=\"ro_example\" -->VALUE<!-- /obs-ref:use -->\n\n   - inner text `obs-ref:def` = canonical literal value;\n   - inner text `obs-ref:use` = materialized literal copy;\n   - changing def НЕ обновляет uses автоматически;\n   - Check uses только проверяет stale copies;\n   - update выполняется отдельно и явно;\n   - semantic relation сама по себе НЕ является Reference Object;\n   - одинаковый текст тоже не надо оборачивать в RO,\n     если synchronization не требуется;\n   - нельзя придумывать ID существующего объекта;\n   - при создании нового объекта нужен один def\n     и согласованная запись registry.\n\n3. Затем открой только:\n\n   .linked-notes/reference-objects.json\n\n   Нужно понять роль registry:\n   он хранит routing/index metadata,\n   а не canonical literal value.\n\n   Посмотри его CURRENT состояние.\n   Не принимай примерные IDs из документации за существующие live objects.\n\nПосле этого остановись.\nНе читай implementation/runtime docs Linked Notes,\nесли задача специально не требует разработки самого инструмента.\n\nПосле чтения дай короткую assimilation summary:\n\n1. Что такое Reference Object.\n2. Чем `obs-ref:def` отличается от `obs-ref:use`.\n3. Где находится настоящий source of truth.\n4. Что хранит `reference-objects.json`.\n5. Как выглядит точный marker syntax.\n6. Что происходит после изменения definition.\n7. Почему RO — не semantic dependency/link graph.\n8. Когда обычный plain text правильнее RO.\n9. Есть ли сейчас live Reference Objects в registry.\n\nОсобенно сохрани это правило для дальнейшей работы:\n\nReference Object использовать только тогда,\nкогда один canonical literal value намеренно материализован\nв нескольких местах и эти копии должны проверяться\nна устаревание/синхронизироваться.\n\nНе использовать Reference Object просто для обозначения:\n“этот объект связан с тем”,\n“этот reference относится к этому priority”,\nили “эти два файла семантически зависят друг от друга”.\n\nЕсли позже работа касается project Reference Research,\nсначала отдельно определить semantic architecture\n(priority → applicable references → local rationale),\nи только затем помечать как RO те конкретные куски,\nкоторые действительно должны быть literal synchronized copies.",
  "createdAt": "2026-08-15T23:11:00.651Z",
  "updatedAt": "2026-08-15T23:11:00.651Z"
}
[/PLANNING_HELPER_LIBRARY_ITEM]
