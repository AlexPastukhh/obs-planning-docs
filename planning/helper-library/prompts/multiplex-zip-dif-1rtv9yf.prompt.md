# Prompt — multiplex zip dif

Status: active Planning Helper library item
Scope: exact insertion text; not planning-command authority.

[PLANNING_HELPER_LIBRARY_ITEM]
{
  "schemaVersion": 1,
  "kind": "prompt",
  "id": "multiplex-zip-dif-1rtv9yf",
  "title": "multiplex zip dif",
  "text": "& { $ErrorActionPreference=\"Stop\"; Set-Location -LiteralPath \"C:\\multiplex\"; $Dirty=@(git status --porcelain=v1 --untracked-files=all); if ($LASTEXITCODE -ne 0) { throw \"git status failed\" }; if ($Dirty.Count -ne 0) { throw \"Working tree is not clean. Snapshot aborted.`n$($Dirty -join \"`n\")\" }; $Head=\"$(git rev-parse HEAD)\".Trim(); if ($LASTEXITCODE -ne 0 -or [string]::IsNullOrWhiteSpace($Head)) { throw \"Cannot determine HEAD\" }; $Parent=\"$(git rev-parse HEAD~1)\".Trim(); if ($LASTEXITCODE -ne 0 -or [string]::IsNullOrWhiteSpace($Parent)) { throw \"Cannot determine HEAD~1\" }; $Branch=\"$(git branch --show-current)\".Trim(); if ($LASTEXITCODE -ne 0 -or [string]::IsNullOrWhiteSpace($Branch)) { throw \"Cannot determine current branch\" }; $RepoName=\"multiplex-main\"; $Stamp=Get-Date -Format \"yyyyMMdd-HHmmss\"; $Zip=Join-Path (Join-Path $HOME \"Downloads\") (\"multiplex-main-snapshot-\"+$Stamp+\".zip\"); $Tmp=Join-Path ([System.IO.Path]::GetTempPath()) (\"multiplex-snapshot-\"+[guid]::NewGuid().ToString(\"N\")); $RepoCopy=Join-Path $Tmp $RepoName; $Utf8=[System.Text.UTF8Encoding]::new($false); New-Item -ItemType Directory -Path $RepoCopy -Force | Out-Null; try { $Tracked=@(git ls-files); if ($LASTEXITCODE -ne 0 -or $Tracked.Count -eq 0) { throw \"git ls-files failed or returned no tracked files\" }; foreach ($Rel in $Tracked) { $Src=Join-Path (Get-Location) ($Rel -replace '/','\\'); if (!(Test-Path -LiteralPath $Src -PathType Leaf)) { throw \"Tracked file missing from working tree: $Rel\" }; $Dst=Join-Path $RepoCopy ($Rel -replace '/','\\'); $Dir=Split-Path -Parent $Dst; if (!(Test-Path -LiteralPath $Dir)) { New-Item -ItemType Directory -Path $Dir -Force | Out-Null }; Copy-Item -LiteralPath $Src -Destination $Dst -Force }; $DiffHead=Join-Path $Tmp \"DIFF_HEAD.patch\"; $DiffWorktree=Join-Path $Tmp \"DIFF_WORKTREE.patch\"; $DiffStaged=Join-Path $Tmp \"DIFF_STAGED.patch\"; git --no-pager diff --no-color --binary HEAD~1 HEAD --output=$DiffHead; if ($LASTEXITCODE -ne 0) { throw \"Failed to create DIFF_HEAD.patch\" }; git --no-pager diff --no-color --binary --output=$DiffWorktree; if ($LASTEXITCODE -ne 0) { throw \"Failed to create DIFF_WORKTREE.patch\" }; git --no-pager diff --cached --no-color --binary --output=$DiffStaged; if ($LASTEXITCODE -ne 0) { throw \"Failed to create DIFF_STAGED.patch\" }; if ((Get-Item -LiteralPath $DiffWorktree).Length -ne 0 -or (Get-Item -LiteralPath $DiffStaged).Length -ne 0) { throw \"Unexpected non-empty worktree/staged diff; snapshot aborted\" }; $Subject=((git log -1 --pretty=%s HEAD) -join \" \").Trim(); if ($LASTEXITCODE -ne 0) { throw \"git log failed\" }; [System.IO.File]::WriteAllText((Join-Path $Tmp \"HEAD.txt\"),(\"branch=$Branch`r`nhead=$Head`r`nparent=$Parent`r`ncommit_subject=$Subject`r`naccepted_diff=HEAD~1..HEAD`r`n\"),$Utf8); [System.IO.File]::WriteAllText((Join-Path $Tmp \"STATUS.txt\"),(((git status --short --branch) -join \"`r`n\")+\"`r`n\"),$Utf8); [System.IO.File]::WriteAllText((Join-Path $Tmp \"SNAPSHOT_INFO.txt\"),(\"format=multiplex-local-snapshot-v1`r`nrepo_folder=$RepoName`r`nsource=C:\\multiplex`r`nbranch=$Branch`r`nhead=$Head`r`naccepted_diff_file=DIFF_HEAD.patch`r`naccepted_diff_range=HEAD~1..HEAD`r`nworktree_diff_file=DIFF_WORKTREE.patch`r`nstaged_diff_file=DIFF_STAGED.patch`r`nrepo_contents=git-tracked-files-only`r`ncreated_local=\"+(Get-Date -Format \"yyyy-MM-ddTHH:mm:ssK\")+\"`r`n\"),$Utf8); if (Test-Path -LiteralPath $Zip) { throw \"Destination already exists: $Zip\" }; Compress-Archive -Path (Join-Path $Tmp \"*\") -DestinationPath $Zip -CompressionLevel Optimal; if (!(Test-Path -LiteralPath $Zip -PathType Leaf)) { throw \"ZIP was not created\" }; $Sha=(Get-FileHash -LiteralPath $Zip -Algorithm SHA256).Hash.ToLowerInvariant(); Write-Host \"Snapshot created:\" $Zip; Write-Host \"SHA-256:\" $Sha; Write-Host \"HEAD:\" $Head; Write-Host \"Accepted diff: DIFF_HEAD.patch = HEAD~1..HEAD\"; Write-Host \"ZIP root: multiplex-main/ + DIFF_HEAD.patch + DIFF_WORKTREE.patch + DIFF_STAGED.patch + HEAD.txt + STATUS.txt + SNAPSHOT_INFO.txt\" } finally { Remove-Item -LiteralPath $Tmp -Recurse -Force -ErrorAction SilentlyContinue } }",
  "createdAt": "2026-08-16T21:45:35.455Z",
  "updatedAt": "2026-08-16T21:45:35.455Z"
}
[/PLANNING_HELPER_LIBRARY_ITEM]
